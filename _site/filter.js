// console.log("Example is up now..")
// var Twit = require('twit');
// var config = require('./config')
// var T = new Twit(config);
//The text of the tweet (status.text).


// def filter_tweet(tweet):
//     //Remove any tweets that don't match our criteria.
//     if not tweet_matches_criteria(tweet):
//         return
//     //Process the remaining tweets.
//     process_tweet(tweet)

// def process_tweet(tweet):
//     //Annotate the tweet dictionary with any other information we need.
//     tweet["sentiment"] = get_sentiment(tweet)
//     //Store the tweet.
//     store_tweet(tweet)
    
// def store_tweet(tweet):
//     //Saves a tweet for later processing.

// def on_status(self, status):
//     if status.retweeted_status:
//         return
//     print(status.text)

// import pandas as pd

// tweets = pd.read_csv("tweets.csv")
// tweets.head()


// def get_attribute(row):
//     shapeAttributes = []
//     text = row["text"].lower()
//     if "rectangle" in text or "square" in text or "quad" in text:
//         shapes.append("clinton")
//     if "rgb(0,0,0)" in text or "rgb(255,255,255)" in text:
//         shapeAttributes.append("trump")
//     return ",".join(shapeAttributes)

// tweets["shapeAttributes"] = tweets.apply(get_attribute,axis=1)
